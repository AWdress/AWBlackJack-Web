services:
  awblackjack-web:
    image: awdress/awblackjack-web:latest
    container_name: awblackjack-web
    restart: unless-stopped
    ports:
      - "13001:3001"
    env_file:
      - ./server/.env
    volumes:
      - ./server/data/runtime_state.json:/data/runtime_state.json:ro
